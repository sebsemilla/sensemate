# Dependencias
node_modules/

# Variables de entorno y secretos
.env
.env.*

# Estado del bot de LinkedIn (no commitear tokens ni textos)
.linkedin_state.json

# Base de datos local
lingua_users.db
*.db

# Archivos pesados / binarios
*.tar.gz
*.zip
android-studio-*
commandlinetools-*
android/

# Python virtualenv
venv/
kyutai-env/

# Certificados SSL locales
localhost+*.pem
*.pem
*.key

# Audio de prueba
test-audio.mp3
samples/

# Archivos de build / capacitor
www/
android/

# OS
.DS_Store
Thumbs.db

# Incluir explícitamente el directorio de discovery MCP (empieza con punto)
!.well-known/

# Logs
*.log
npm-debug.log*

# Datos de runtime de bots (generados en producción, no commitear)
bots.json
feed_posts.json
classroom_classes.json

# Archivos de progreso de generación de misiones
grupos_tarjetas/**/.progress_*.json
gen_progress_status.json
gen_en_pt_pids.json

# Scripts de generación locales (no son parte del servidor)
generate_missions.js
generate_vocab_contextual.js
gen_*.sh
run_level.sh
complete_parciales.sh
resume_generation.sh
monitor_and_launch.sh
enrich_*.js
